# Bot Configuration
BOT_USERNAME=mybot

# Slack (optional) — via Vercel Connect (https://vercel.com/docs/connect)
# Set to your Slack connector UID; the adapter resolves a short-lived bot token
# at runtime and verifies Connect-forwarded webhooks with a Vercel OIDC token.
# SLACK_CONNECTOR=slack/your-connector
# Requires VERCEL_OIDC_TOKEN in the environment — run `vercel env pull` locally.

# Microsoft Teams (optional)
# TEAMS_APP_ID=your-app-id
# TEAMS_APP_PASSWORD=your-app-password

# Google Chat (optional)
# GOOGLE_CHAT_CREDENTIALS={"type":"service_account",...}

# Discord (optional)
# DISCORD_BOT_TOKEN=your-bot-token
# DISCORD_PUBLIC_KEY=your-public-key

# Facebook Messenger (optional)
# FACEBOOK_APP_SECRET=your-app-secret
# FACEBOOK_PAGE_ACCESS_TOKEN=your-page-access-token
# FACEBOOK_VERIFY_TOKEN=your-verify-token

# GitHub (optional) — via Vercel Connect (https://vercel.com/docs/connect)
# Set to your GitHub connector UID; the adapter resolves a short-lived
# installation token at runtime and verifies Connect-forwarded webhooks with a
# Vercel OIDC token (no GitHub App private key or webhook secret to store).
# GITHUB_CONNECTOR=github/your-connector
# Requires VERCEL_OIDC_TOKEN in the environment — run `vercel env pull` locally.
# Strongly recommended (required on serverless): the bot's numeric user id.
# In Connect mode the adapter can't auto-detect it, so without this it can't
# recognize its own comments and will reply to itself in a loop across
# function instances. Find it (no auth needed) with:
#   curl -s 'https://api.github.com/users/<your-app-slug>[bot]'
# GITHUB_BOT_USER_ID=123456

# Linear (optional) — via Vercel Connect (https://vercel.com/docs/connect)
# Set to your Linear connector UID; the adapter resolves a short-lived access
# token at runtime and verifies Connect-forwarded webhooks with a Vercel OIDC
# token (no Linear API key or webhook secret to store).
# LINEAR_CONNECTOR=linear/your-connector
# Requires VERCEL_OIDC_TOKEN in the environment — run `vercel env pull` locally.
# Optional inbound webhook mode (use agent-sessions for app-actor installs):
# LINEAR_MODE=comments
# LINEAR_BOT_USERNAME=my-bot

# Notion (optional) — access-token connection + webhook subscription
# 1. Create a connection at https://app.notion.com/developers/connections
#    (Access token auth, single workspace — installed automatically)
# 2. Copy Access token → NOTION_TOKEN; enable Read/Insert comments
# 3. Content access tab: enable the pages/databases to serve
# 4. Start the app + ngrok, create a Webhooks subscription to
#    https://<ngrok>/api/webhooks/notion (Comment events only)
# 5. Copy the verification_token from server logs into Notion Verify +
#    NOTION_VERIFICATION_TOKEN, then restart (URL locks after verify)
# NOTION_TOKEN=ntn_...
# NOTION_VERIFICATION_TOKEN=secret_...
# Optional: mention (default) | all-comments | keyword
# For local testing without @-mentions, use all-comments:
# NOTION_MENTION_MODE=all-comments
# NOTION_KEYWORDS=@docs-bot,hey bot
# NOTION_BOT_USERNAME=docs-bot

# Redis (for production state persistence)
# REDIS_URL=redis://localhost:6379
