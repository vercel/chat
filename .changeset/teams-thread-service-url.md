---
"@chat-adapter/teams": patch
---

Send, edit, delete, react, and type against the service URL encoded in the Teams thread ID instead of the app-wide default, so conversations hosted on regional or sovereign Bot Framework endpoints are reached correctly.
