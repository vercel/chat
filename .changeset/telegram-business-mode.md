---
"@chat-adapter/telegram": minor
---

Add Telegram Business mode support (`business_connection`, `business_message`, and `business_connection_id` on outbound API calls). Opt in via `businessMode: true`.
