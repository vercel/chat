---
"@chat-adapter/linear": patch
"create-chat-sdk": minor
---

Generate Linear Connect bots with explicit agent-session mode and setup guidance. Recommend agent sessions once during initialization when a token resolver and webhook verifier are configured without a mode, while preserving the adapter's comments default.
